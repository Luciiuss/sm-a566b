diff -Naur a/fs/open.c b/fs/open.c
--- a/fs/open.c	2025-07-08 21:38:48.715680349 +0200
+++ b/fs/open.c	2025-07-08 21:43:51.487222107 +0200
@@ -543,8 +543,16 @@
 	return res;
 }
 
+#ifdef CONFIG_KSU
+extern __attribute__((hot, always_inline)) int ksu_handle_faccessat(int *dfd, const char __user **filename_user, int *mode,
+																	int *flags);
+#endif
+
 SYSCALL_DEFINE3(faccessat, int, dfd, const char __user *, filename, int, mode)
 {
+#ifdef CONFIG_KSU
+	ksu_handle_faccessat(&dfd, &filename, &mode, 0);
+#endif
 	return do_faccessat(dfd, filename, mode, 0);
 }
 
